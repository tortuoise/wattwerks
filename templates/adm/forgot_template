<section class="standart">
            	<div class="form-inline">
                    <form enctype="multipart/form-data"  id="userforgot" name="userforgot">
                        <p>Enter the e-mail address associated with your account. Click submit to have your password e-mailed to you.</p>
                        <h2>Your E-Mail Address</h2>
                        <div class="field">
                            <label>E-Mail Address:</label>
                            <input type="text" value="" name="email">
                        </div>
                        <div class="field">
                            <label>First Name:</label>
                            <input type="text" value="" name="firstname">
                        </div>
                        <div class="field">
                            <a class="button" href="/account/login">Back</a>&nbsp;&nbsp;
                            <a onclick="$(this).parents('form').submit();" class="button button-alt">Continue</a>
                        </div>
                    </form>
                </div>
</section>
